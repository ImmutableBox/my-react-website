import React, {Component} from 'react';

/**
 * Internal projects component. This component contains projects I have worked on internally on the website.
 */
class Internal extends Component {
  render() {
    return (
      <p>Internal Projects</p>
    );
  }
}

export default Internal;
